name: Docker

on:
  workflow_call: 
jobs:

  docker:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set Docker Build
    - name: Docker Login
      uses: docker/login-action@v2.1.0
      with:
        username: josealceu # user on DockerHub
        password: ${{secrets.password}} # optional

      
    
